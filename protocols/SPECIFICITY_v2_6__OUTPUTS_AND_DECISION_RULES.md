# Outputs and decision rules

Execution PASS requires 864/864 new non-PE attack cells with valid checkpoint/reference/operator/protocol provenance, finite full-validation accuracy and canonical ADS, and lossless delta/per-image artifact hashes.

Damage-matched PASS requires 5/10/20 pp estimable without extrapolation for all 96 PE×seed×surface units per target. Severe 50%-clean is secondary and may be non-common.

Do not promote fixed-epsilon ADS/Drop at .2 to a specificity claim; use matched-damage comparisons as primary.
